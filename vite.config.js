
export default {
  // Base public path when served in production
  base: './',
  // Configure the build output directory
  build: {
    outDir: 'dist',
  },
  // Configure the server
  server: {
    port: 3000,
    open: true,
  }
};
